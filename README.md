# React Frontend Demo

Initial setup for a React + TypeScript + Vite app with:

- Tailwind CSS configured for shadcn-style components
- Framer Motion for animations
- A reusable `Button` primitive and `cn()` utility
- Path alias (`@/`) support

## Quick start

```bash
npm install
npm run dev
```
